import{notFound}from"next/navigation";import{solutions}from"@/content/site";import{Breadcrumbs,Button,CTA,FAQ,SectionHeading}from"@/components/ui";import{SolutionCard}from"@/components/cards";import{metadata as meta}from"@/lib/seo";export function generateStaticParams(){return solutions.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=solutions.find(x=>x.slug===slug);return s?meta(s.title,s.challenge):{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=solutions.find(x=>x.slug===slug);if(!s)notFound();return <><section className="page-hero"><div className="container"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Solutions",href:"/solutions"},{label:s.title}]}/><span className="tag">Editable solution category · not a finalized product</span><h1>{s.title}</h1><p className="hero-copy">{s.challenge}</p><div className="actions"><Button href="/book">Request a consultation</Button><Button href="#preview" secondary>View interface concept</Button></div></div></section><section id="preview"><div className="container grid grid-2"><div className="system" aria-label="Abstract interface preview"><div className="dashboard"><small>INTERFACE PREVIEW</small><div className="dashrow"/><div className="dashrow"/><div className="dashrow"/></div></div><div><div className="eyebrow">Business problem</div><h2>Information should move with the work.</h2><p>This solution category can be configured around agreed workflows, user roles, reporting needs, and operating constraints. The preview is illustrative rather than a finished product screen.</p><h3>Intended audiences</h3><div className="audiences">{s.industries.map(x=><span className="pill" key={x}>{x}</span>)}</div></div></div></section><section className="band"><div className="container"><SectionHeading eyebrow="Core features" title="A focused foundation, shaped to fit."/><div className="grid grid-3">{s.features.map(x=><div className="card" key={x}><h3>{x}</h3><p>Final behavior, permissions, integrations, and data structure are defined through discovery.</p></div>)}</div></div></section><section><div className="container grid grid-2"><div><h2>How the workflow can operate</h2>{["Capture information once","Route work to the right role","Track status and exceptions","Use reports to guide action"].map(x=><div className="step" key={x}><div><h3>{x}</h3></div></div>)}</div><div><h2>Integrations, security & data</h2><p>Connections to approved APIs, databases, payment services, or other platforms are assessed individually. Access controls, data handling, backups, logging, hosting, and retention requirements are agreed for the project rather than assumed.</p><div className="notice"><strong>Pricing:</strong> Request pricing. Scope and commercial terms require discovery; no fixed pricing has been supplied.</div><h2>FAQs</h2><FAQ/></div></div></section><section><div className="container"><h2>Related solution categories</h2><div className="grid grid-3">{solutions.filter(x=>x.slug!==s.slug).slice(0,3).map(x=><SolutionCard solution={x} key={x.slug}/>)}</div></div></section><CTA/></>}
