import{notFound}from"next/navigation";import{services}from"@/content/site";import{Breadcrumbs,Button,CTA,FAQ,SectionHeading}from"@/components/ui";import{ServiceCard}from"@/components/cards";import{metadata as meta}from"@/lib/seo";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);return s?meta(s.title,s.short):{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const related=services.filter(x=>x.slug!==s.slug).slice(0,3);return <><section className="page-hero"><div className="container"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Services",href:"/services"},{label:s.title}]}/><div className="eyebrow">Service</div><h1>{s.intro}</h1><p className="hero-copy">{s.short}</p><Button href="/book">Discuss this service</Button></div></section><section><div className="container grid grid-2"><div><h2>What this solves</h2>{s.problems.map(x=><div className="step" key={x}><div><h3>{x}</h3></div></div>)}</div><div><h2>Typical capabilities</h2><div className="grid">{s.capabilities.map(x=><div className="card" key={x}><h3>{x}</h3><p>Selected and adapted according to agreed requirements, users, risk, and operating context.</p></div>)}</div></div></div></section><section className="band"><div className="container grid grid-2"><div><div className="eyebrow">Who it is for</div><h2>Organizations ready to replace friction with a clearer system.</h2><p>This service can support businesses, institutions, public-sector teams, NGOs, startups, and other organizations where the challenge and scope are a suitable fit.</p></div><div><h3>Example use cases</h3>{s.useCases.map(x=><p className="pill" key={x}>{x}</p>)}</div></div></section><section><div className="container"><SectionHeading eyebrow="Delivery" title="A practical route to a dependable result."/><div className="grid grid-3">{["Discovery and requirements","Design and incremental development","Testing, deployment, documentation and support"].map(x=><div className="card" key={x}><h3>{x}</h3><p>Scope, outputs, responsibilities, and review points are agreed for the engagement.</p></div>)}</div><h2 style={{marginTop:"5rem"}}>Expected deliverables</h2><p>Depending on scope: requirements documentation, experience and interface designs, configured or developed software, testing records, deployment materials, user or administrator guidance, training, handover, and an agreed maintenance plan.</p><h2 style={{marginTop:"5rem"}}>Frequently asked questions</h2><FAQ/><h2 style={{marginTop:"5rem"}}>Related services</h2><div className="grid grid-3">{related.map((x,i)=><ServiceCard service={x} index={i} key={x.slug}/>)}</div></div></section><CTA/></>}
