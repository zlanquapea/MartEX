import type{MetadataRoute}from"next";import{services,solutions}from"@/content/site";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const paths=["","/about","/services","/solutions","/work","/work/connected-operations-concept","/process","/book","/contact","/privacy","/terms",...services.map(s=>`/services/${s.slug}`),...solutions.map(s=>`/solutions/${s.slug}`)];return paths.map(url=>({url:base+url,lastModified:new Date(),changeFrequency:"monthly",priority:url===""?1:.7}))}
