import Link from "next/link";import {ArrowUpRight} from "lucide-react";import type {Service,Solution} from "@/content/site";
export function ServiceCard({service,index}: {service:Service;index:number}){return <Link className="card" href={`/services/${service.slug}`}><div className="card-number">0{index+1}</div><h3>{service.title}</h3><p>{service.short}</p><ArrowUpRight aria-hidden="true"/></Link>}
export function SolutionCard({solution}:{solution:Solution}){return <Link className="card" href={`/solutions/${solution.slug}`}><span className="tag">Editable solution category</span><h3>{solution.title}</h3><p>{solution.challenge}</p><div className="audiences">{solution.industries.map(x=><span className="pill" key={x}>{x}</span>)}</div></Link>}
export function CaseCard(){return <article className="card"><span className="tag">Concept solution</span><h3>Connected operations workspace</h3><p>A capability demonstration showing how records, approvals, and reporting can become one clear operational flow. It is not a client project.</p><Link href="/work/connected-operations-concept">Explore the concept →</Link></article>}
