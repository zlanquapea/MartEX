import Link from "next/link"; import { ArrowRight } from "lucide-react";
export function Button({href,children,secondary=false}:{href:string;children:React.ReactNode;secondary?:boolean}){return <Link className={`btn ${secondary?"btn-ghost":"btn-primary"}`} href={href}>{children}<ArrowRight size={16}/></Link>}
export function SectionHeading({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="section-head"><div><div className="eyebrow">{eyebrow}</div><h2>{title}</h2></div>{copy&&<p>{copy}</p>}</div>}
export function Breadcrumbs({items}:{items:{label:string;href?:string}[]}){return <nav className="breadcrumbs" aria-label="Breadcrumb">{items.map((x,i)=><span key={x.label}>{i>0&&" / "}{x.href?<Link href={x.href}>{x.label}</Link>:x.label}</span>)}</nav>}
export function CTA(){return <section><div className="container"><div className="cta"><div className="eyebrow">Begin with the problem</div><h2>Let’s turn your business challenge into a working digital solution.</h2><div className="actions"><Button href="/book">Book a Consultation</Button><Button href="/contact" secondary>Start a Conversation</Button></div></div></div></section>}
export function FAQ({items}:{items?:[string,string][]}){const list=items??[["How does an engagement begin?","We begin by understanding the operational challenge, users, constraints, and intended outcome before recommending a solution."],["Can MartEX support the system after launch?","Yes. Maintenance arrangements can cover monitoring, updates, troubleshooting, performance improvements, support, and planned enhancements."],["How is project scope agreed?","Requirements, deliverables, review points, responsibilities, and change arrangements are documented and agreed before delivery begins."]];return <div className="accordion">{list.map(([q,a])=><details key={q}><summary>{q}</summary><p>{a}</p></details>)}</div>}
