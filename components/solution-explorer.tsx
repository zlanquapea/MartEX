"use client";import {useState}from"react";import{solutions}from"@/content/site";import{SolutionCard}from"./cards";
export function SolutionExplorer(){const options=["All",...Array.from(new Set(solutions.flatMap(s=>s.industries)))];const[filter,setFilter]=useState("All");const shown=filter==="All"?solutions:solutions.filter(s=>s.industries.includes(filter));return <><div className="solution-filter" aria-label="Filter solutions">{options.map(x=><button className="btn btn-ghost" aria-pressed={filter===x} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div><div className="grid grid-3">{shown.map(s=><SolutionCard solution={s} key={s.slug}/>)}</div></>}
