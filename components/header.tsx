"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {Menu,X} from "lucide-react";import {ThemeSelector} from "./theme";
const links=[["Services","/services"],["Solutions","/solutions"],["Work","/work"],["About","/about"],["Process","/process"],["Contact","/contact"]];
export function Header(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>16);f();addEventListener("scroll",f,{passive:true});return()=>removeEventListener("scroll",f)},[]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <header className={`header ${scrolled?"scrolled":""}`}><nav className="container nav" aria-label="Primary"><Link className="brand" href="/" onClick={()=>setOpen(false)}>Mart<span>EX</span></Link><div className="navlinks">{links.map(([a,b])=><Link key={b} href={b}>{a}</Link>)}<ThemeSelector/><Link className="btn btn-primary" href="/book">Book a Consultation</Link></div><button className="iconbtn mobile-toggle" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open?"Close menu":"Open menu"}>{open?<X/>:<Menu/>}</button><div id="mobile-menu" className={`mobile-panel ${open?"open":""}`}>{links.map(([a,b])=><Link key={b} href={b} onClick={()=>setOpen(false)}>{a}</Link>)}<Link href="/book" onClick={()=>setOpen(false)}>Book a Consultation</Link><ThemeSelector/></div></nav></header>}
